整体要求如下：

1. 如果要执行python代码，注意要激活conda环境 ppmat
2. 生成代码的时候，要精简，没有明确要求，不要生成注释。代码中不要有任何的emoji
3. 如果涉及到timm和huggingface，必须带上环境变量 HF_ENDPOINT=https://hf-mirror.com
4. 没有明确要求，不写注释，不写文档，不要出现emoji
5. 如果gpu暂时不可用，也要使用gpu，不要自行切换到cpu代码模式